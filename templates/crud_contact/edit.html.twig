{% extends 'base.html.twig' %}

{% block title %}Editer le contact{% endblock %}

{% block body %}
    <h1>Editer le contact</h1>

    {{ include('crud_contact/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('c_r_u_d_contact_index') }}" class="btn btn-primary">Retour à la liste des contactes</a>

    {{ include('crud_contact/_delete_form.html.twig') }}
{% endblock %}
