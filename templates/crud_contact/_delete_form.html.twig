<form method="post" action="{{ path('c_r_u_d_contact_delete', {'id': contact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">
    <button type="submit" value="Send" class="btn btn-primary py-3 px-5" style="background-color: #F45F5F; color: white; margin-bottom: 20px; margin-top: 20px">Supprimer</button>
</form>
