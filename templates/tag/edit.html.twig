{% extends 'base.html.twig' %}

{% block title %}Editer un tag{% endblock %}

{% block body %}
    <h1>Editer un tag</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('tag_index') }}"class="btn btn-primary" >Retour à la liste des Tags</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
